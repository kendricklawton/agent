[package]
name    = "agent-collector"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
agent-core.workspace   = true
nvml-wrapper.workspace = true   # NVML via dlopen — no NVIDIA library needed at build time

[lints]
workspace = true
