# Stable toolchain for the whole workspace — plain Rust on `std`, one host target. No nightly and no
# special compile targets (that was the old eBPF project).
[toolchain]
channel    = "1.94.1"
components = ["rustfmt", "clippy"]
profile    = "minimal"
